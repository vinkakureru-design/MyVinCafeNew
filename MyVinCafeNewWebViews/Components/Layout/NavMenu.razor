<nav class="sidebar">
    <div class="sidebar-header">
        <h4><i class="fas fa-campground"></i> MyVinCafe</h4>
        <span class="small">Admin Panel</span>
    </div>
    <div class="mt-4">
        <!-- href="" mengarah ke halaman Dashboard (Index) -->
        <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
            <i class="fas fa-th-large"></i> <span>Dashboard</span>
        </NavLink>
        
        <!-- href="employees" mengarah ke Employees.razor -->
        <NavLink class="nav-link" href="employees" Match="NavLinkMatch.All">
            <i class="fas fa-users-cog"></i> <span>Manage Employees</span>
        </NavLink>
        
        <NavLink class="nav-link" href="members" Match="NavLinkMatch.All"y>
            <i class="fas fa-id-card"></i> <span>Members</span>
        </NavLink>
        
        <hr class="mx-3">
        <button class="nav-link text-danger bg-transparent border-0 w-100 text-start" @onclick="Logout">
            <i class="fas fa-sign-out-alt"></i> <span>Keluar</span>
        </button>
    </div>
</nav>

@code {
    private void Logout()
    {
        // Logika logout C# akan diletakkan di sini nantinya
        Console.WriteLine("User logged out");
    }
}